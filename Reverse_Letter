function reverseLetter(str) {
  var nstr = "";
  var letters = /^[A-Za-z]+$/;
  for(var i = str.length - 1; i >= 0; i--) {
    console.log(str[i]);
    if (str[i].match(letters)) {
      nstr += str[i];
    }
  }
  return nstr;
}
